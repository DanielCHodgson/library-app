export default Book;

export default class Book {

    constructor(title, author, description, published) {
        this.title = title;
        this.author = author;
        this.description = description;
        this.published = published
    }

}